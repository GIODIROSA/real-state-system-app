# App Directory

This directory follows the Next.js App Router structure.

## Structure

- **(auth)**: Route group for authentication pages (Login, Register).
- **(dashboard)**: Route group for protected dashboard pages.
- **layout.tsx**: Root layout and nested layouts.
- **page.tsx**: Page components for each route.
