# Hooks Directory

This directory contains custom React hooks.

## Purpose

- Encapsulate reusable logic.
- Abstract complex state management or side effects.
- Examples: `useAuth`, `useFetch`, `useWindowSize`.
